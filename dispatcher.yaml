receive_inbound_connectors:
  - messenger_transport
  - whatsapp_transport

receive_outbound_connectors:
  - faqbrowser

routing_table:
  messenger_transport:
    default: ["faqbrowser", "default"]

  whatsapp_transport:
    default: ["faqbrowser", "whatsapp"]

  faqbrowser:
    default: ["messenger_transport", "default"]
    whatsapp: ["whatsapp_transport", "default"]
